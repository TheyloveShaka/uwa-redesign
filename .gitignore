# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files
.env
.env.*
!.env.example

# Coverage / build artifacts
coverage
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local tool config
.claude/

# Untouched image sources, kept locally so optimisation is repeatable
.image-originals/

# Watermarked Adobe Stock comp used as a local stand-in for the hero.
# Deliberately NOT committed: it carries a visible AdobeStock watermark and is
# licensed for comping only. It was already scrubbed out of git history once.
# Replace with the licensed master, then remove this rule.
public/parks/hero.mp4
